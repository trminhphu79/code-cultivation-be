# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build code-cultivation-be`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t code-cultivation-be`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system code-cultivation-be && \
          adduser --system -G code-cultivation-be code-cultivation-be

COPY dist/apps/code-cultivation-be code-cultivation-be/
RUN chown -R code-cultivation-be:code-cultivation-be .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix code-cultivation-be --omit=dev -f install

CMD [ "node", "code-cultivation-be" ]
