import { MicroServiceName } from '..';

export const Scripture = Object.freeze({
  Quiz: `${MicroServiceName.Scripture}/Quiz`,
  General: `${MicroServiceName.Scripture}/General`,
  Feedback: `${MicroServiceName.Scripture}/Feedback`,
});
